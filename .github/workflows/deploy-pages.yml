name: Deploy GitHub Pages

on:
  push:
    branches: [main, claude/vietnam-driving-license-quiz-p6nlz3]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      # Cần bật trước: Settings → Pages → Source: GitHub Actions (repo public hoặc gói trả phí).
      - name: Cấu hình Pages
        id: pages
        uses: actions/configure-pages@v5
      - run: npm ci
      - name: Build trang tĩnh
        run: npm run build
        env:
          # https://<user>.github.io/<repo>/ → basePath = /<repo>
          NEXT_PUBLIC_BASE_PATH: ${{ steps.pages.outputs.base_path || '/vietnam-drive-license' }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
